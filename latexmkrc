# Reproducible XeLaTeX defaults. Makefile supplies job name and pre-TeX data.
$pdf_mode = 5;
$xelatex = 'xelatex -no-shell-escape %O %S';
$max_repeat = 5;
$recorder = 1;
$cleanup_includes_cusdep_generated = 1;
